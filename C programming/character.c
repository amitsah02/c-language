#include<file.h>
void main ()
{
    FILE*fptr ;
    char ch ;
    fptr=fopen("kamlesh txt","w");
    while ()
    {ch=fgetc(fptr)
if(ch==EOF)
{break;
{else
printf ("%c",ch);}
fclose(fptr);
}